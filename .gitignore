# 환경변수 — API 키는 절대 커밋하지 않는다
.env
.env.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# 수집 원본과 캐시 — 스크립트로 다시 받을 수 있다
data/raw/
data/cache/

# SQLite DB — sql/*.sql 과 14_build_db.py 로 언제든 재생성된다
*.db
*.sqlite
*.sqlite3

# OS
Thumbs.db
Desktop.ini
.DS_Store
