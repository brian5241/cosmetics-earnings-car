# 실적 발표 항목이 주가에 미치는 영향 분석 — 설계 기록

> 화장품 섹터 대상 이벤트 스터디 프로젝트의 설계 의사결정 기록.
> 코드를 작성하기 전에 확정한 내용과 그 근거를 남긴다.
> README 작성 시 이 문서를 원본으로 사용한다.

- 작성일: 2026-08-21
- 목적: 데이터 분석 직군 지원용 포트폴리오
- 핵심 소구점: 도메인 지식이 아니라 **수집·정합성 검증·전처리 파이프라인 설계 역량**

---

## 1. 연구 질문

> 실적 발표에 담긴 여러 항목 중, 발표 직후 주가 반응과 관련이 큰 항목은 무엇인가?

부차 질문:
- 시장은 외형 성장(매출)에 반응하는가, 수익성 개선(마진)에 반응하는가?
- 발표 전 선반영 정도가 발표 후 반응 크기를 줄이는가?

---

## 2. 분석 설계

### 2.1 종속변수: CAR (Cumulative Abnormal Return, 누적초과수익률)

발표 직후 주가 움직임에서 **시장 전체 요인을 제거한 몫**만 남긴 값.

```
AR  = 실제 수익률 − 기대 수익률(α + β × 시장수익률)
CAR = 이벤트 윈도우 구간의 AR 합계
```

| 구분 | 설정 |
|---|---|
| 주력 윈도우 | CAR[0, +1] |
| 보조 윈도우 | CAR[0, +5], CAR[0, +20] |
| 추정 윈도우 | [-250, -30] |
| 벤치마크 | 시장지수 + 섹터지수 |

**왜 [0,+1]인가**
하루만 보면 우연에 휘둘리고, 장 마감 후 발표는 반응이 다음 거래일에 나온다. 이틀을 묶어 둘 다 흡수한다.

**왜 추정 윈도우에서 -30일을 빼는가**
발표 직전 구간은 이미 실적 기대감에 오염되어 있다. 이 구간으로 베타를 추정하면 "정상 수익률" 기준 자체가 왜곡된다.

**왜 섹터지수를 함께 쓰는가**
시장 요인만 제거하면 섹터 공통 요인이 남는다. 발표일이 특정 시기에 몰려 있어 이 요인은 상쇄되지 않는다.

### 2.2 무엇을 통제하고 무엇을 통제하지 않는가

판단 기준: **랜덤이면 통제하지 않고, 체계적이면 통제한다.**

| 교란 요인 | 성격 | 처리 |
|---|---|---|
| 시장 전체 등락 | 체계적 (전 종목 공통) | 시장모델로 제거 |
| 섹터 업황 뉴스 | 체계적 (섹터 공통) | 섹터지수 팩터 추가 |
| 실적과 동시 발표된 배당·자사주 공시 | 체계적 (실적과 동반 경향) | 더미 처리 또는 표본 제외 |
| 발표일 우연히 나온 개별 종목 뉴스 | 랜덤 | 통제하지 않음 — 표본 평균이 상쇄 |

개별 이벤트마다 "왜 올랐는가"를 설명할 필요가 없다. 답하려는 질문은 "이 종목이 왜 올랐나"가 아니라 **"특정 항목이 좋은 그룹은 평균적으로 더 오르는가"**이기 때문이다. 표본이 충분하면 랜덤 교란은 평균에서 사라진다.

**예외**: 랜덤 교란이라도 대형 M&A·유상증자 같은 극단 케이스는 평균을 끌고 간다. CAR 상하위 이벤트는 개별 확인 후 실적 무관 대형 이벤트면 제외 또는 윈저라이징.

---

## 3. 유니버스 선정

### 3.1 정의 확정 과정

섹터 정의를 두 가지로 놓고 비교했다.

| | 협의 (화장품) | 광의 (K뷰티) |
|---|---|---|
| 범위 | 브랜드 · ODM · 유통 | + 메디컬에스테틱 · 미용기기 · 톡신 |
| 종목 수 | 11 | 20+ |
| 실적 동인 | 소비 수요로 통일 | 소비 수요 / 장비 판매 / 허가·소송 혼재 |

**광의 채택 시 문제**: 톡신·미용기기 종목은 실적보다 소송·허가 뉴스에 주가가 크게 반응한다. 이는 랜덤 노이즈가 아니라 해당 종목군에 체계적으로 붙어 있는 요인이므로 "표본 평균이 상쇄한다"는 논리가 성립하지 않는다. 섹터를 한정한 목적 자체가 무너진다.

**결론**: v1은 협의(화장품)로 확정. 광의 확장은 v2에서 별도 비교 분석으로 수행.

### 3.2 최종 정의

> **화장품 섹터**: 화장품의 제조(ODM/OEM), 브랜드 판매, 유통을 주된 사업으로 하는 상장사.
> 화장품·뷰티 제품 매출이 전사 매출의 과반인 기업으로 한정한다.

**포함 기준**
1. 화장품·뷰티케어 매출이 전사 매출의 과반
2. 2024년 상반기 이전 상장 (분석 기간 내 최소 4개 분기 확보)
3. 지주사–사업회사 중복 시 사업회사만 채택

**제외 사례와 사유**

| 종목 | 사유 |
|---|---|
| LG생활건강, 애경산업 | 생활용품·음료 비중이 커 실적 동인이 화장품 외 요인에 좌우됨 |
| 아모레G | 아모레퍼시픽과 실적 발표 중복 (동일 이벤트 이중 계상) |
| 파마리서치, 휴젤, 메디톡스, 클래시스 | 화장품 밸류체인 밖 (의료기기·의약품) |
| 에이피알 외 최근 상장주 중 일부 | 베타 추정용 과거 주가 부족 |

### 3.3 최종 유니버스 (11종목)

| 유형 | 종목 | 코드 | 시장 |
|---|---|---|---|
| 브랜드 | 아모레퍼시픽 | 090430 | KOSPI |
| 브랜드 | 에이피알 | 278470 | KOSPI |
| 브랜드 | 클리오 | 237880 | KOSDAQ |
| 브랜드 | 브이티 | 018290 | KOSDAQ |
| 브랜드 | 네오팜 | 092730 | KOSDAQ |
| 브랜드 | 달바글로벌 | 483650 | KOSPI |
| ODM | 한국콜마 | 161890 | KOSPI |
| ODM | 코스맥스 | 192820 | KOSPI |
| ODM | 코스메카코리아 | 241710 | KOSDAQ |
| 유통 | 실리콘투 | 257720 | KOSDAQ |
| 유통 | 아이패밀리에스씨 | 114840 | KOSDAQ |

> 종목코드·상장일·시총·상장 상태는 pykrx로 검증 예정 (미완료).

**경계 사례 (기준 충족하나 순수하지 않음 — 수집 후 매출 구성 확인 필요)**

- 한국콜마: 화장품 ODM 본업이나 제약 CMO 부문이 연결에 포함
- 브이티: 화장품 본업이나 과거 포장재 사업 잔존, 기간 초반과 후반의 사업 구조 상이 가능
- 아이패밀리에스씨: 화장품 본업이나 웨딩 사업 잔존
- 실리콘투: 취급 품목은 전량 화장품이나 업태상 유통업으로 분류될 수 있음 → 밸류체인 포함으로 예외 처리

**근거 보강 방침**: 거래소 업종 분류(WICS/FICS)를 출발점으로 삼고, 벗어난 케이스는 예외 사유를 명시한다. "임의로 골랐다"가 아니라 "공식 분류를 기준으로 삼고 근거를 남겨 조정했다"가 되도록 한다.

---

## 4. 분석 기간

**4년 (16분기)** — 종목별 상장 이력에 따라 불균형 패널.

예상 이벤트 수: 이력 긴 9종목 × 16분기 ≈ 144, 짧은 종목 ≈ 15~20, **합계 약 150~160**.

### 왜 1년이 아닌가
15종목 × 4분기 = 60 이벤트로는 독립변수 5개만 넘어가도 유의성이 나오지 않는다. 수집 코드는 동일하므로 기간 확장 비용이 거의 없다.

### 왜 4년에서 멈추는가
- DART 구조화 재무 데이터 하한이 2015 사업연도라 물리적으로는 10년까지 가능
- 그러나 기간이 길수록 레짐 변화 통제 부담이 커진다
- 4년이 표본 수와 관리 가능성의 균형점

### 4년으로 늘리며 발생하는 문제와 대응

| 문제 | 대응 |
|---|---|
| 레짐 변화 (업/다운사이클 혼재로 계수 상쇄) | **분기 고정효과 또는 레짐 더미 필수** |
| 생존 편향 | 현재 시점이 아닌 **과거 시점 기준으로 유니버스 구성** |
| 분할·합병·회계 재작성으로 재무 시계열 단절 | 이상치 자동 제거가 아니라 **원인 확인 후 개별 처리** |
| 상장 이력 짧은 종목의 베타 추정 불가 | **시장조정수익률 모델**로 폴백 (아래 참조) |

### 상장 이력이 짧은 종목 처리

에이피알(2024년 초), 달바글로벌(2024년 말)은 추정 윈도우 250거래일을 확보할 수 없다.

1. **시장조정수익률 모델 폴백** — 베타를 1로 고정하고 `AR = 종목수익률 − 시장수익률`로 계산. 추정 구간이 불필요하다. 두 모델을 모두 돌려 결과를 비교하면 강건성 검증이 된다.
2. **상장 후 첫 2개 분기 이벤트 제외** — 유통물량 부족, 과도한 변동성, 락업 해제 등으로 주가가 정상 상태가 아니다.

---

## 5. 데이터 소스

| 데이터 | 소스 | 가용 기간 | 비용 |
|---|---|---|---|
| 주가·거래량·시총 | pykrx / FinanceDataReader | 사실상 무제한 | 무료 |
| 공시 목록·접수시각 | DART OpenAPI | 1999~ | 무료 |
| 구조화 재무제표 | DART OpenAPI | **2015 사업연도~** | 무료 |
| 컨센서스(추정치) | — | **과거 시점 확보 불가** | — |

### 5.1 컨센서스를 사용하지 않는 이유 — 데이터 누수

무료/유료의 문제가 아니라 **시점**의 문제다.

네이버 금융·에프앤가이드 무료 페이지에서 컨센서스를 조회할 수 있으나, 표시되는 값은 **현재 시점 기준으로 갱신된 값**이다. "2023년 3분기 발표 직전에 시장이 기대한 값"은 어디에도 남아 있지 않다.

갱신된 현재값을 과거 이벤트에 결합하면 **look-ahead bias / data leakage**가 된다. 미래 정보로 과거를 설명하는 셈이다.

유료 단말(DataGuide, 퀀티와이즈)은 추정치를 시점별로 아카이빙하므로 과거 임의 시점의 컨센서스를 복원할 수 있다. 접근이 없으므로 사용하지 않는다.

**대안**: 컨센서스 대신 기대치 대리변수를 직접 구성한다.
- 과거 4분기 추세로 예측한 값 대비 실제 편차 (naive earnings expectation model)
- 발표 전 CAR[-20,-1] — 시장이 미리 반영한 기대의 직접 측정치

### 5.2 선반영을 오히려 이용한다

주가가 발표 전에 기대를 반영해뒀다면, 발표 직후 [0,+1] 구간에 남는 움직임은 정확히 **기대와 달랐던 부분**에 대한 반응이다. 예상대로 나온 실적은 발표 당일 주가를 움직이지 않는다.

즉 **CAR[0,+1] 자체가 이미 서프라이즈의 측정치**다. 컨센서스 숫자 없이도 시장이 대신 계산해준 셈이다.

**대가**: 실적 항목을 "예상된 부분 / 놀라운 부분"으로 분해할 수 없다. 시장이 이미 예상한 성장이 독립변수에 그대로 들어가므로 **회귀계수가 실제보다 작게 추정된다(희석)**. 유의성은 나오되 효과 크기는 과소평가된다. 한계로 명시한다.

**추가 활용**: CAR[-20,-1]을 독립변수로 투입하면 선반영을 통제하는 동시에 측정할 수 있다. 계수가 음수로 나오면 선반영이 작동한다는 직접 증거이며, 그 자체로 보고할 결과가 된다.

---

## 6. 특성(독립변수) 설계

### 6.1 원천 데이터 제약

DART 주요계정 API가 제공하는 항목은 다음뿐이다.

```
매출액, 영업이익, 당기순이익, 자산총계, 부채총계, 자본총계
```

판관비·재고자산·R&D비 등 세부 항목은 전체 재무제표 API가 필요하며 파싱 부담이 크다. v1은 주요계정으로 한정한다.

### 6.2 영업이익 성장률을 제외한 이유 — 계산식이 깨지는 지점

**문제 1: 분모가 음수일 때 부호가 역전된다**

기저가 적자면 관행적인 YoY 성장률 공식이 방향을 반대로 기록한다.

| 케이스 | 실제 의미 | `(t − t₄)/t₄` |
|---|---|---|
| −10억 → −20억 | 적자 2배 악화 | **+100%** (개선으로 기록) |
| −10억 → −5억 | 적자 절반 축소 | **−50%** (악화로 기록) |

정보가 없는 것이 아니라 **있는 정보가 반대로 들어간다**. 그리고 적자→적자 케이스는 영향이 작지 않다. 적자 축소는 턴어라운드 기대로 읽혀 주가가 크게 오르기도 한다. 단순히 결측 처리하면 **반응이 가장 큰 케이스를 골라서 버리는** 결과가 된다.

**문제 2: 분모가 0에 가까우면 값이 폭발한다**

절댓값 분모 `(t − t₄)/|t₄|`를 쓰면 문제 1은 해결되지만, 기저가 작으면 여전히 터진다.

| 케이스 (매출 1,000억 가정) | 실제 의미 | 원 공식 | 절댓값 분모 | 매출 대비 |
|---|---|---|---|---|
| −10억 → −20억 | 악화 | +100% ✗ | −100% ✓ | −1.0%p ✓ |
| −10억 → −5억 | 개선 | −50% ✗ | +50% ✓ | +0.5%p ✓ |
| −0.1억 → +3억 | 흑자전환 | +3100% ✗ | +3100% ✗ | +0.31%p ✓ |

이는 적자 문제가 아니라 **작은 분모 문제**다. 흑자 0.2억 → 5억도 +2400%가 된다.

**판단 경로**: 원 공식 → 부호 역전 발견 → 절댓값 분모 → 폭발 문제 잔존 → **매출액 정규화 채택**

**해결**: 영업이익 YoY 성장률을 특성에서 제외하고, 다음으로 분해한다.

```
영업이익 변화 ≈ 매출 성장(외형) + 마진 변화(수익성)
```

- **매출액 YoY 성장률** — 매출은 음수가 될 수 없어 부호 문제가 원천적으로 없음
- **OPM 변화폭 (%p)** — 비율의 차이라 적자여도 계산 가능. −5% → −2%면 +3%p로 정확히 개선으로 기록

얻는 것:
1. 적자 기저 결측이 거의 사라져 표본을 버리지 않는다
2. 매출 YoY와 영업이익 YoY의 다중공선성이 해소된다
3. "외형 성장이 중요한가, 마진 개선이 중요한가"로 해석이 선명해진다

### 6.3 최종 특성 목록 (8개)

| # | 특성 | 계산 | 역할 |
|---|---|---|---|
| 1 | 매출액 YoY 성장률 | (t − t₄) / t₄ | 핵심 — 외형 |
| 2 | OPM 변화폭 (%p) | OPM_t − OPM_t₄ | 핵심 — 수익성 |
| 3 | 영업이익 추세이탈도 | (실제 OP − 추세예측 OP) / 매출액 | 핵심 — 서프라이즈 대리 |
| 4 | 흑자전환 더미 | 부호 전환 여부 | 핵심 — 질적 사건 |
| 5 | 적자전환 더미 | 부호 전환 여부 | 핵심 — 질적 사건 |
| 6 | 발표 전 CAR[-20,-1] | 이벤트 스터디 산출 | 통제 — 선반영 |
| 7 | 로그 자산총계 ※ | ln(당분기 자산총계) | 통제 — 규모 |
| 8 | 정보출처 더미 ※※ | 잠정실적 / 정기보고서 | 통제 — 정보 신선도 |

전환 더미(4, 5)는 연속 지표로 표현되지 않는 **질적 사건**을 포착하기 위해 별도로 둔다.

**※ 규모 통제변수 변경 (2026-08-21)**: 당초 로그 시가총액을 사용할 계획이었으나, 이벤트 시점 시가총액을 무료로 확보할 수 없어 **로그 자산총계로 대체**한다. 상세는 14.2 참조. 최종 확정 대기.

**※※ 장마감 후 발표 더미 → 정보출처 더미 (2026-08-21)**: OpenDART가 접수'일자'만 제공하고 시각을 주지 않아 장중/장후 구분이 불가능하다. 대신 잠정실적/정기보고서 구분을 통제변수로 쓴다. 상세는 14.5 참조.

**다중공선성 주의**: 1·2·3은 서로 상관이 있다. 일부러 남겨두고 Lasso가 무엇을 죽이는지 관찰하는 것이 이 분석의 관전 포인트다. VIF를 반드시 확인한다.

### 6.4 전처리 결정 사항

| 항목 | 처리 |
|---|---|
| **4분기 단독 실적** | DART에는 4분기 항목이 없다. `연간(사업보고서) − 3분기 누계`로 역산 |
| **발표 시각 기준일 보정** | 접수시각 15:30 이후면 t=0을 다음 거래일로 이동 |
| **잠정실적 / 정기보고서 중복** | 잠정실적 우선, 없으면 정기보고서. 중복 제거 규칙 사전 확정 |
| **연결 vs 별도** | 연결(CFS) 고정, 없는 기업만 별도로 폴백 |
| **계정과목명 불일치** | "매출액 / 수익(매출액) / 영업수익" 등이 기업·연도별로 상이 → 외부 매핑 테이블로 표준화 |
| **극단값** | 1%/99% 윈저라이징. **자르기 전 상하위 값을 눈으로 확인**해 진짜 이벤트인지 데이터 오류인지 구분 |
| **결측** | 적자 기저 결측은 6.2 설계로 대부분 소거. 잔여 결측은 사유별로 기록 |

---

## 7. 모델

| 모델 | 목적 |
|---|---|
| **OLS** | 해석 — 계수 방향과 유의성 |
| **Lasso / Ridge** | 변수 선택 및 예측력 검증 |

### 7.1 왜 Lasso인가
- 표본 대비 변수가 많은 상황에서 정규화가 정석
- 재무 지표 간 다중공선성이 심함
- Lasso는 계수를 0으로 죽여 변수를 선택한다 → **"어떤 항목이 중요한가"라는 원 질문과 목적이 정확히 일치**
- 스케일에 민감하므로 피처 스케일링 필수

### 7.2 왜 부스팅을 쓰지 않는가

트리 앙상블 경험칙은 변수 1개당 관측치 50~100개다. 특성 8개면 **최소 400~800**, 여유 있게는 2,000 이상이 필요하다. 현재 표본은 약 150개다.

추가로, 주가 수익률은 신호 대비 잡음이 극단적으로 크다(R² 0.05면 양호한 수준). 이런 저신호 환경에서 부스팅은 신호 대신 노이즈를 학습한다. 금융 분야 ML 연구가 수십만 관측치를 쓰는 이유다.

> 표본이 150건이라 트리 앙상블은 과적합 위험이 크다. 정규화 선형모델이 이 표본 크기와 다중공선성 구조에 더 적합하다고 판단해 배제했다.

### 7.3 교차검증 — 두 번째 데이터 누수 지점

**일반 K-fold를 쓰면 안 된다.** 랜덤 분할 시 2025년 데이터로 학습해 2022년을 예측하는 상황이 발생한다. 미래 정보로 과거를 맞히는 누수다.

`TimeSeriesSplit`을 사용해 앞 기간으로 학습하고 뒤 기간으로 검증한다.

**누수 원칙이 파이프라인 양쪽 끝에서 일관되게 적용된다**
1. 수집 단계 — 컨센서스 시점 문제로 소스를 포기 (5.1)
2. 모델링 단계 — 시계열 특성 때문에 검증 전략을 변경 (7.3)

---

## 8. 로드맵

| | 범위 | 이벤트 | 분석 | 소요 |
|---|---|---|---|---|
| **v1** | 화장품 11종목 × 4년 | 약 150 | OLS + Lasso | 3일 |
| **v2** | K뷰티 20종목+ × 4년 | 약 320 | + 유형 더미, 상호작용, 정의 민감도 | +2~3일 |
| v3 (선택) | 코스피200 × 10년 | 약 8,000 | + 부스팅 | 1~2주 |

**v2와 v3의 확장 축은 다르다.** v2는 *정의* 확장(분석 질문 심화), v3는 *규모* 확장(모델 복잡도 확보). 섞지 않는다.

**v2에서 새로 가능한 것**: 브랜드 / ODM / 유통 / 메디컬 / 기기 유형 더미와 상호작용항. "어떤 항목이 중요한가"에서 **"어떤 사업 모델에서 어떤 항목이 중요한가"**로 질문이 한 단계 올라간다.

**v2에서도 부스팅은 불가**: 320개는 여전히 부족하다.

### v1 설계 조건 (v2 비용을 줄이기 위해 처음부터 지킬 것)

1. **종목 리스트를 코드 밖 CSV로 관리** — `포함버전` 컬럼으로 v1/v2 필터 전환, `유형` 컬럼이 v2에서 바로 더미가 됨

   ```
   종목코드, 종목명, 시장, 유형, 상장일, 포함버전
   090430, 아모레퍼시픽, KOSPI, 브랜드, ..., v1
   214450, 파마리서치,   KOSDAQ, 메디컬, ..., v2
   ```

2. **계정과목 매핑 테이블도 외부 파일** — 종목 추가 시 매핑만 추가

3. **DB 스키마에 유형 컬럼을 미리 포함** — v2에서 마이그레이션 불필요

---

## 9. 구현 방침

### 9.1 파이프라인

```
DART 수집 → 계정과목 표준화 → SQLite 적재 → 주가 수집
→ CAR 계산 → EDA → OLS → Lasso(TimeSeriesSplit CV) → 시각화
```

### 9.2 DB 스키마 (SQLite)

```
events  (종목코드, 분기, 발표일시, 매출액, 영업이익, 당기순이익, ...)
prices  (종목코드, 일자, 종가, 수익률)
market  (일자, 지수종가, 지수수익률)
```

SQL 레이어를 두는 이유: 데이터 분석 직군 공고 대부분이 SQL을 요구하나 pandas만 쓴 포트폴리오는 SQL 역량을 보여주지 못한다. 추가 비용 2~3시간.

### 9.3 코드 작성 원칙

- 설정값(종목, 기간, 윈도우, API 키)은 파일 상단 한 곳에 집중
- 함수 하나가 한 가지 일만 수행
- 단계별 중간 결과를 파일로 저장 (재실행 시 앞단 재수집 불필요, 눈으로 검증 가능)
- 단계마다 `shape`, `head()`, 결측 수 출력
- 주석은 **무엇을**과 **왜**를 구분해 기록. "왜" 주석이 면접 답변의 원본이 된다
- 학습 중에는 주석을 넉넉히 달고, 마지막 정리 단계에서 자명한 것을 걷어낸다

---

## 10. 일정

### 10.1 단계별 소요 (초보 기준, 총 30~38시간)

| 단계 | 시간 | 난이도 |
|---|---|---|
| pykrx 종목 검증 | 1h | 낮음 |
| DART 키 발급 + `corp_code` 매핑 | 1~2h | 중 (첫 관문) |
| 공시 목록 수집 (발표일시 포함) | 2h | 중 |
| 재무제표 수집 | 2h | 낮음 |
| **계정과목 표준화** | **3~4h** | **높음 — 최대 병목** |
| 4분기 역산 + 검증 | 2h | 중 |
| SQLite 스키마 + 적재 | 2h | 낮음 |
| 주가·지수 수집 | 1~2h | 낮음 |
| 베타 추정 + AR/CAR 계산 | 3h | 중 |
| 특성 생성 (8개) | 2~3h | 중 |
| EDA + 이상치 개별 확인 | 2h | 중 |
| OLS | 1~2h | 낮음 |
| Lasso + TimeSeriesSplit | 2h | 중 |
| 시각화 | 2~3h | 낮음 |
| README + 코드 정리 | 4~6h | 중 |

| 하루 투입 | 총 소요 |
|---|---|
| 8시간 | 4~5일 |
| 4~5시간 | 7~9일 |
| 2~3시간 | 2주 |

**시간 배분 주의**: 전체의 약 7할이 수집·정제에 소요된다. 분석 코드는 짧다.

**범위 축소가 필요할 경우 잘라낼 순서**: 보조 윈도우(-1h) → 종목 축소(-3h) → Lasso(-2h) → SQL 레이어(-2h).
SQL 레이어는 직군 특성상 마지막까지 남긴다.

### 10.2 세션 분할

대화가 길어지면 앞부분 맥락이 압축되므로 세션을 단계별로 끊는다.
새 세션은 `design-log.md`를 먼저 읽고 시작한다.

| 세션 | 범위 |
|---|---|
| 1 | 환경 구축 + 유니버스 검증 ✅ 완료 |
| 2 | DART 수집 (`corp_code` 매핑, 공시 목록, 재무제표) |
| 3 | 계정과목 표준화 + 4분기 역산 + SQLite 적재 |
| 4 | 주가 수집 + 베타 추정 + CAR 계산 |
| 5 | 특성 생성 + EDA |
| 6 | OLS + Lasso + 시각화 |
| 7 | README + 코드 정리 |

초보 기준으로 시간을 잡아먹는 지점:
- DART 고유번호 매핑 (`corp_code.xml` 다운로드·파싱 필요, 종목코드로 직접 조회 불가)
- 계정과목명 불일치 표준화
- 연결/별도 혼재 처리
- 4분기 역산 시 음수 영업이익 발생 케이스의 진위 확인

---

## 11. 포트폴리오 서술 방침 (데이터 분석 직군)

주식은 **소재**이고, 파는 것은 **데이터 파이프라인 구축 역량**이다. "주가 분석"으로 소개하면 면접관이 평가할 기준을 갖지 못한다.

| 원래 강조하려던 것 | 바꿔서 강조할 것 |
|---|---|
| 이벤트 스터디 방법론 | 시점 정합성 있는 패널 데이터 설계 |
| CAR, 초과수익률 | 노이즈 제거를 위한 벤치마크 차감 (한 줄로) |
| 어떤 재무 항목이 유의한가 | 이종 소스 조인 + 스키마 표준화 파이프라인 |
| 회귀 결과 해석 | 결측·이상치 판단 근거와 처리 이력 |

**최대 강점은 데이터 누수 서사**다. 수집 단계와 모델링 단계 양쪽에서 같은 원칙을 일관되게 적용한 경험은 교과서적 지식과 구별된다.

**30초 소개문**

> 기업 실적 발표 데이터와 주가 데이터를 결합해서, 어떤 재무 지표가 발표 직후 주가 반응과 관련이 큰지 분석했습니다. 데이터가 두 개 소스로 나뉘어 있고 발표 시각 단위로 정확히 맞춰야 해서, 수집·정합성 검증·표준화 파이프라인을 직접 설계한 게 이 프로젝트의 핵심이었습니다.

방법론은 상대가 물으면 그때 설명한다.

**주의**: 본인이 설명하지 못할 기법은 넣지 않는다. 회귀 하나를 제대로 해석하는 편이 복잡한 모델을 붙이고 얼버무리는 것보다 낫다.

---

## 12. 명시할 한계

1. 컨센서스 부재로 실적을 "예상된 부분 / 놀라운 부분"으로 분해할 수 없어 **회귀계수가 과소추정**될 수 있다
2. 표본 약 150건으로 통계적 검정력이 제한적이다
3. 단일 섹터 분석이므로 결과를 다른 섹터로 일반화할 수 없다
4. 일부 종목은 화장품 외 잔여 사업을 보유한다 (3.3 경계 사례)
5. 상장 이력이 짧은 종목은 시장조정수익률 모델로 폴백해 다른 종목과 산출 방식이 다르다

---

## 13. 미결 사항

**남은 사항 (v1 분석에는 영향 없음)**
- [ ] 거래소 업종 분류(WICS/FICS) 대조 및 예외 사유 정리 — 종목 선정 근거 보강용
- [ ] 파마리서치 부록 분석 포함 여부 결정 (데이터는 수집돼 있음, v2 후보)
- [ ] 달바글로벌 최종 처리 — 유효 3건. 현재 포함 상태이며 제외 모형도 강건성으로 돌려둠(결과 불변)

**완료**
- [x] 실행 환경 구축 (2026-08-21)
- [x] 종목코드·종목명 유효성 검증 (2026-08-21)
- [x] 주가 가용 구간 확인 (2026-08-21)
- [x] DART OpenAPI 키 발급 및 연결 확인 (2026-08-21)
- [x] DART 고유번호(corp_code) 매핑 — 12종목 전부 성공 (2026-08-21)
- [x] 공시 목록 수집 및 이벤트 정의 확정 — v1 160건 (2026-08-21)
- [x] 잠정실적 / 정기보고서 중복 제거 규칙 확정 (14.5)
- [x] 재무제표 수집 및 분기 패널 구성 — 246행, 결측 0, 검산 통과 (2026-08-21)
- [x] 규모 통제변수 확정 — 로그 자산총계 채택 (2026-08-21)
- [x] 섹터 지수 구성 — 유니버스 동일가중, 자기 제외, 시장에 직교화 (14.10)
- [x] 베타 추정 가능 여부 이벤트 단위 판정 구현 — 폴백 2건 (14.10)
- [x] 상장 직후 2개 분기 제외 적용 — 160 → 155건 (14.12)
- [x] 주가 수집 및 CAR 계산·검증 (14.10, 14.11)
- [x] 특성 확정 및 최종 데이터셋 구성 — 155건 (14.12)
- [x] OLS + Lasso 분석 및 강건성 검증 (14.13)
- [x] 결과 시각화 4종 (14.14)
- [x] README 작성 (2026-08-21)

---

## 14. 실행 기록 및 설계 조정

> 계획 대비 실제로 확인된 사실과 그에 따른 조정을 기록한다.
> 계획 문서를 덮어쓰지 않고 차이를 남기는 것이 목적이다.

### 14.1 환경 (2026-08-21)

| 항목 | 상태 |
|---|---|
| Python | 3.12.10 |
| pandas / numpy | 3.0.3 / 2.4.6 |
| statsmodels / scikit-learn | 설치됨 |
| pykrx | 신규 설치 |
| FinanceDataReader | 0.9.202 신규 설치 |

파일 구조:

```
cosmetics-earnings-car/
├── docs/design-log.md
├── src/01_check_universe.py
└── data/universe.csv
```

### 14.2 pykrx 시가총액 조회 불가 → 규모 통제변수 변경

`pykrx.stock.get_market_cap()`이 KRX 로그인을 요구하도록 변경되어 동작하지 않는다.

```
KRX 로그인 실패: KRX_ID 또는 KRX_PW 환경 변수가 설정되지 않았습니다.
Error occurred in get_market_cap_by_ticker: Expecting value: line 1 column 1
```

주가(OHLCV) 조회는 정상이므로 주가 수집에는 영향이 없다.
`FinanceDataReader.StockListing("KRX")`가 시총(Marcap)·상장주식수(Stocks)를 주지만 **현재 시점 스냅샷**이라 이벤트 시점 시총은 얻을 수 없다.

**검토한 선택지**

| 방안 | 비용 | 문제 |
|---|---|---|
| KRX 계정 생성 후 환경변수 설정 | 회원가입 | 로그인 방식 변경·차단 시 재현 불가 |
| 현재 상장주식수 × 과거 종가로 근사 | 0 | 증자·분할 이력이 있으면 왜곡 |
| **로그 자산총계로 대체 (채택 예정)** | **0** | 시장 평가가 아닌 장부 규모 |

**채택 근거**: 자산총계는 DART 주요계정에 이미 포함되어 추가 수집이 없고, **동일 분기 재무제표 값이라 시점 정합성이 완벽**하다. 근사나 우회가 아니다. 통제변수의 목적(규모 구분)은 충분히 달성된다.

README 기재 문안:

> 이벤트 시점 시가총액은 데이터 제약으로 확보가 어려워, 동일 분기 재무제표의 자산총계를 규모 대리변수로 사용했다. 시장 평가가 아닌 장부 기준이라는 한계가 있으나 시점 정합성은 보장된다.

### 14.3 유니버스 검증 결과 (2026-08-21)

12종목 전부 종목코드·종목명 일치. 시가총액 분포는 0.16조 ~ 14.9조로 약 90배 차이 — 규모 효과 통제에 충분한 분산.

| 종목 | 코드 | 시장 | 시가총액 | 조회시작일 | 거래일수 |
|---|---|---|---|---|---|
| 에이피알 | 278470 | KOSPI | 14.9조 | 2024-02-27 | 598 |
| 아모레퍼시픽 | 090430 | KOSPI | 7.9조 | (한도) | 3000 |
| 달바글로벌 | 483650 | KOSPI | 3.3조 | **2025-05-22** | **300** |
| 한국콜마 | 161890 | KOSPI | 3.1조 | (한도) | 3000 |
| 실리콘투 | 257720 | KOSDAQ | 3.0조 | 2021-09-29 | 1192 |
| 코스맥스 | 192820 | KOSPI | 2.8조 | (한도) | 3000 |
| 코스메카코리아 | 241710 | KOSDAQ GLOBAL | 1.3조 | 2016-10-28 | 2401 |
| 브이티 | 018290 | KOSDAQ | 0.52조 | (한도) | 3000 |
| 네오팜 | 092730 | KOSDAQ | 0.40조 | (한도) | 3000 |
| 클리오 | 237880 | KOSDAQ | 0.22조 | 2016-11-09 | 2393 |
| 아이패밀리에스씨 | 114840 | KOSDAQ | 0.16조 | 2021-10-28 | 1173 |
| 파마리서치 (v2) | 214450 | KOSDAQ GLOBAL | — | 2015-07-24 | 2711 |

**주의**: FDR은 최대 3000행만 반환한다. 거래일수가 정확히 3000인 종목의 "조회시작일"은 실제 상장일이 아니다(아모레퍼시픽은 2006년 상장). 분석에 필요한 구간은 2021-06 이후이므로 영향은 없다.

**달바글로벌 문제**: 설계 시점에는 2024년 말 상장으로 알고 있었으나 **실제 상장일은 2025-05-22**다. 거래일 300일로, 4년 분석 기간 내 이벤트가 4~5개이며 상장 후 첫 2분기 제외 규칙(4절)을 적용하면 **2~3개만 남는다.** 표본 기여가 사실상 없다. 포함 여부 재결정 필요.

**에이피알**: 2024-02-27 상장, 이벤트 약 9개. 첫 2분기 제외 후 7개 내외로 표본에 실질 기여. 유지.

### 14.4 베타 추정 가능 여부는 이벤트 단위로 판정해야 한다

`01_check_universe.py`의 초기 구현은 **종목 단위**로 "주가 확보 여부"를 판정해, 실리콘투(2021-09 상장)와 아이패밀리에스씨(2021-10 상장)를 통째로 시장조정모델 폴백 대상으로 분류했다.

그러나 실제로는 **2022년 하반기 초기 이벤트 몇 건만** 추정 구간 [-250, -30]이 부족하고, 이후 이벤트는 정상적으로 시장모델을 적용할 수 있다. 종목 전체를 폴백시키면 불필요하게 정보를 버린다.

**조정**: CAR 계산 단계에서 이벤트별로 추정 구간 확보 여부를 검사하고, 부족한 개별 이벤트만 시장조정모델로 처리한다. 어떤 이벤트에 어느 모델을 썼는지 컬럼으로 기록해 결과 해석 시 구분할 수 있게 한다.

### 14.5 이벤트 정의 확정 — 접수시각 부재와 잠정/정기 혼합

**접수시각을 얻을 수 없다.** OpenDART `list.json` 의 `rcept_dt` 는 `20260730` 형태로 **날짜만** 제공한다. 접수시각 필드가 없다.

→ 특성 8번(장마감 후 발표 더미)을 만들 수 없다. **정보출처 더미로 대체**한다(6.3 ※※). CAR[0,+1] 윈도우가 장중/장후 차이를 상당 부분 흡수하도록 이미 설계돼 있어 손실이 크지 않다. 시각이 필요하면 거래소 KIND 크롤링이 가능하나 v2로 미룬다.

**잠정실적 공시는 회사 재량이라 관행이 갈린다.**

| 패턴 | 종목 |
|---|---|
| 매 분기 공시 (16/16) | 아모레퍼시픽, 클리오, 네오팜, 코스메카코리아, 한국콜마 |
| 부분 공시 | 코스맥스(11), 파마리서치(12), 에이피알(10), 실리콘투(7) |
| 결산만 공시 | 브이티(4), 아이패밀리에스씨(3) |

잠정실적만으로는 표본의 절반이 날아간다. 정기보고서는 법정 의무라 종목당 16~19건으로 빠짐이 없다.

**확정한 이벤트 구성 규칙**

1. 분석 단위는 종목 × 분기. 한 분기에 이벤트 하나.
2. `t=0` 은 **그 분기 실적이 처음 공개된 날** — 대상 분기별 최소 접수일자.
3. `[기재정정]` 제외 (원공시 존재).
4. 같은 날 복수 공시는 우선순위로 1건만: 연결잠정 > 별도잠정 > 손익변동 > 정기보고서.
5. **정보출처(잠정실적/정기보고서)를 기록**해 통제변수로 사용.

중복이 발생하는 실제 사례:
- 한국콜마는 같은 날 「영업(잠정)실적」(별도)과 「연결재무제표기준영업(잠정)실적」을 둘 다 공시
- 결산 시점에는 잠정실적과 「매출액또는손익구조 30% 이상 변동」이 같은 날 겹침

**상장 전 이벤트 제외**: 비상장 시기에도 정기보고서를 제출한 기업이 있다(에이피알 6건, 달바글로벌 3건). 주가가 없으므로 제외했다. 168 → 160건.

**결과**: v1 이벤트 **160건** / 176분기 (91%). 정보출처는 잠정실적 132, 정기보고서 44.
정보출처가 종목 **내부에서도** 분기마다 변동하므로(브이티·실리콘투·코스맥스), 종목 고정효과와 함께 써도 식별이 가능하다.

**달바글로벌**: 이벤트 5건. 상장 직후 2개 분기를 제외하면 **유효 3건**. 제외 권고.

### 14.6 계정과목 표준화가 불필요했다 — 예상 빗나감

설계 시 "회사·연도별 계정과목명 불일치(매출액 / 수익(매출액) / 영업수익)"를 최대 병목(3~4시간)으로 잡았다.

실제 수집 결과 **12종목 전부 동일한 명칭**을 사용했다.

```
손익계산서: 매출액 12 / 영업이익 12 / 당기순이익(손실) 12
재무상태표: 자산총계 12 / 부채총계 12 / 자본총계 12
```

**이유**: `fnlttSinglAcnt`(주요계정 API)는 DART가 이미 표준화한 계정만 반환한다. 명칭이 제각각인 문제는 `fnlttSinglAcntAll`(전체 재무제표)이나 원본 XBRL을 쓸 때 발생한다.

세부 항목(판관비·재고자산·R&D)을 포기하고 주요계정으로 한정한 6.1의 결정이 결과적으로 3~4시간을 절약했다. **전체 추정 30~38h → 27~34h.**

### 14.7 분기 금액 구조 확인 및 4분기 역산 검증

탐색으로 확인한 `fnlttSinglAcnt` 금액 체계:

| 보고서 | `thstrm_amount` | `thstrm_add_amount` |
|---|---|---|
| 1분기 | 해당 3개월 | 누계(=1분기) |
| 반기 | **해당 3개월(Q2)** | 누계(6개월) |
| 3분기 | **해당 3개월(Q3)** | 누계(9개월) |
| 사업보고서 | 연간 | 없음 |

즉 **Q1~Q3는 역산 없이 그대로 사용 가능**하고, Q4만 `연간 − 3분기누계`로 계산한다.

검산 예시 (아모레퍼시픽 2025 매출): `1,067,512 + 1,005,024 + 1,016,904 = 3,089,440` = 3분기 누계와 일치.

**전수 검산**: `Q1+Q2+Q3+Q4 == 연간`을 4개 분기가 모두 있는 54개 종목·연도에 대해 확인 → **불일치 0건**.

재무상태표는 시점 개념이므로 각 보고서의 기말 잔액을 그대로 사용한다.

### 14.8 연결/별도 폴백 및 결측 보완

**연결(CFS) 우선, 없으면 별도(OFS)** 로 폴백하고 `기준` 컬럼에 기록했다. 폴백이 발생한 곳은 네오팜 2건, 아이패밀리에스씨 8건뿐이고 나머지는 전부 연결이다.

**결측 1건 발생 및 복구**: 코스메카코리아 2025Q1 연결 손익계산서에 **매출액 계정 자체가 없었다**(영업이익 등 다른 계정은 정상, 별도에는 존재). DART 측 태깅 누락으로 보인다.

누계 관계로 복원했다.

```
Q1 = 반기누계 − 반기 3개월치
Q2 = 반기누계 − 1분기누계
Q3 = 3분기누계 − 반기누계
```

복원값은 연간 합계 검산을 통과했다(검산 대상 53 → 54건, 불일치 0).

**최종 분기 패널**: 246행, 결측 0. 산출방식은 직접 188 / 역산 57 / 누계보완 1.

### 14.9 진행 현황 (2026-08-21)

```
cosmetics-earnings-car/
├── docs/
│   ├── design-log.md            설계·의사결정·실행 기록
│   └── explainer.md             비전문가용 설명 (계속 갱신)
├── src/
│   ├── config.py                공통 설정 (경로·키·기간·윈도우·종목)
│   ├── 01_check_universe.py     유니버스 검증
│   ├── 02_dart_corpcode.py      DART 연결 확인 + 고유번호 매핑
│   ├── 03_fetch_disclosures.py  공시 목록 수집
│   ├── 04_build_events.py       이벤트 테이블 구성
│   ├── 05_fetch_financials.py   재무제표 원본 수집
│   └── 06_build_panel.py        분기 재무 패널 구성
└── data/
    ├── universe.csv             종목 마스터 (12)
    ├── corp_code_map.csv        고유번호 매핑
    ├── disclosures_raw.csv      공시 원본 (1,453)
    ├── earnings_events.csv      실적 관련 공시 (367)
    ├── events.csv               이벤트 (v1 160)
    ├── financials_quarterly.csv 분기 재무 패널 (246, 결측 0)
    └── raw/                     API 원본 (gitignore)
```

### 14.10 CAR 계산 — 두 건의 설계 오류와 수정

**오류 1: 긴 윈도우 미완성을 이유로 이벤트 전체를 버렸다.**
최초 구현이 `t0 + max(윈도우) >= len(dates)` 로 일괄 제외해, CAR[0,+20]이 아직 채워지지
않은 2026Q2 이벤트 12건에서 계산 가능한 CAR[0,+1]까지 버렸다.
→ 윈도우별로 가용 여부를 개별 판정하도록 수정. 12건 회복.

**오류 2: 섹터 팩터가 시장 팩터를 흡수했다.**
유니버스 동일가중 섹터 지수는 시장 움직임을 이미 포함하므로 두 팩터가 강하게 겹친다.
그대로 회귀하니 **beta_시장 평균이 0.22**로 비정상적으로 낮게 나왔다.
→ 추정 구간에서 섹터를 시장에 회귀한 잔차(직교화)를 쓰도록 수정. **beta_시장 0.73**으로 정상화.

섹터 지수는 대상 종목 자신을 제외하고 구성한다(leave-one-out). 자기가 포함된 지수를
벤치마크로 쓰면 초과수익률이 축소된다.

**모델 선택 결과**: 2팩터 시장모델 174건 / 시장조정모델 폴백 2건(에이피알 2024Q1 관측 13,
달바글로벌 2025Q2 관측 23). 14.4에서 정한 대로 이벤트 단위 판정이 작동했다.

### 14.11 CAR 검증 (09단계)

극단값을 원본 종가로 되짚어 전수 확인했다. 전부 실제 거래였다.

```
실리콘투 2024Q1   t0 +29.82% → t+1 +29.95% (상한가 2연속)  CAR +48.06%
코스메카코리아 2023Q1  t+1 +29.99% (상한가)                 CAR +26.49%
아모레퍼시픽 2024Q2   t+1 -24.91%                          CAR -26.64%
```

CAR[0,+1] 표준편차 11.1%가 큰 이유도 확인됐다.

```
평상시 일간 수익률 표준편차 : 3.46%
발표일 당일 표준편차       : 7.01%   (2.03배)
sqrt(2) x 7.01%          ≈ 9.9%   (실측 11.1%와 정합)
```

**실적 발표일의 변동성이 평상시의 2배**라는 것 자체가 보고할 만한 사실이다.

**주의**: 실리콘투가 극단값을 지배한다(CAR 표준편차 20.5%, 2위 달바 16.1%).
상하위 5건 중 4건이 이 종목이다. 강건성 검증에 이 종목 제외 모형을 포함했다.

### 14.12 특성 확정 — 전환 더미 제외

실제 표본의 전환 사례가 흑자전환 1건, 적자전환 0건, 적자지속 0건이었다.
11종목이 분석 기간 내내 흑자 기조였기 때문이다.
1건짜리 더미는 해당 관측치 하나를 완전히 설명해버려 해롭다. **특성에서 제외**.

적자 기저 대비 설계(6.2)가 이 표본에서는 발동하지 않은 셈이다.
다만 매출YoY + OPM변화 분해 자체는 유효했고, 종목을 넓히는 v2에서는 필요해진다.

**최종 특성 6개**: 매출YoY, OPM변화, 추세이탈도, CAR_PRE, 로그자산, 잠정실적더미
**최종 표본 155건** (v1 160건 → 상장직후 5건 제외)

### 14.13 분석 결과

**OLS (종속변수 CAR[0,+1], 종목 군집 표준오차, n=155, R² 0.146)**

| 변수 | 계수 | p값 | 1 표준편차 효과 |
|---|---|---|---|
| 추세이탈도 | +0.576 | 0.0007 *** | +3.1%p |
| 매출YoY | +0.052 | 0.010 ** | +1.8%p |
| OPM변화 | +0.227 | 0.152 | +1.3%p |
| 로그자산 | +0.004 | 0.501 | +0.5%p |
| 잠정실적더미 | −0.002 | 0.866 | −0.1%p |
| CAR_PRE | −0.012 | 0.832 | −0.1%p |

VIF 전부 1.4 미만. 다중공선성 문제 없음.

**강건성 (추세이탈도 계수)**: 기본 +0.576*** / 윈저라이징 +0.544*** / 실리콘투 제외 +0.493***
/ 달바 제외 +0.545*** / CAR[0,+5] +0.705*** / CAR[0,+20] +0.840**  — 모든 설정에서 유지.

**Lasso (TimeSeriesSplit 5-fold)**: CAR[0,+1]에서 추세이탈도 > 매출YoY > OPM변화 선택,
CAR[0,+5]·CAR[0,+20]에서는 추세이탈도만 생존. **통제변수 3개는 모든 윈도우에서 계수 0.**
목적이 다른 두 방법이 같은 결론에 도달했다.

**해석**

1. 컨센서스 없이 만든 추세이탈도가 가장 강하다. "좋은 실적"이 아니라 "기대보다 좋은 실적"이
   주가를 움직인다는 이론적 예측과 일치한다.
2. 매출YoY는 윈도우가 길어질수록 계수가 커진다(+0.052 → +0.099). PEAD 패턴.
3. OPM변화는 CAR[0,+1]에서 유의하지 않다. CAR[0,+5]에서 +0.275**가 나오나 신뢰구간이
   경계선상이라 강하게 주장하지 않는다.
4. **선반영 가설 기각**: CAR_PRE 계수 −0.012, p=0.83. 발표 전 상승이 발표 후 반응을
   줄인다는 예상이 지지되지 않았다.

### 14.14 시각화 (12단계)

네 장으로 정리. 색은 역할에 따라 배정했다(발산형 파랑↔빨강 = 긍정/부정 서프라이즈,
범주형 슬롯 1~3 = 세 지표). 팔레트는 CVD 분리도·명도대비 검증을 통과했다.

| 그림 | 내용 |
|---|---|
| fig1 | 항목별 효과 크기 (1 표준편차 기준, 95% 신뢰구간) |
| fig2 | 서프라이즈 3분위 그룹의 평균 CAR 궤적 (t-20 ~ t+20) |
| fig3 | 추세이탈도 vs CAR 산점도 |
| fig4 | 윈도우별 계수 변화 (반영 속도) |

fig2가 가장 설득력이 크다. 발표 전까지 세 그룹이 붙어 있다가 **발표일에 갈라지고
되돌아오지 않는다**. 사전 유출이나 선반영이 아니라 발표 자체가 정보를 전달했다는 증거다.

시각화를 위해 08단계에서 이벤트별 AR 경로(t-20~t+20)를 `ar_paths.csv`로 저장하도록 했다.
직교화 계수를 재구성하지 않아도 되게 하려는 것이다.

### 14.15 SQL 레이어 구축 (14단계)

9.2에서 계획했다가 파이프라인이 예상보다 빨리 풀리면서 건너뛴 항목이다.
공개 전 점검에서 **설계 문서가 약속한 것과 저장소 실제가 어긋난다**는 점이 드러나 뒤늦게 채웠다.

기존 파이프라인은 손대지 않고, 산출 CSV를 SQLite에 적재해 같은 계산을 SQL로도 표현하는
층을 얹는 방식으로 구성했다.

**스키마** — 5개 테이블로 정규화. 컬럼명은 영문으로 두어 SQL에서 따옴표 없이 쓰게 했다.

```
stocks(code PK) ─┬─ financials(code, year, quarter PK)
                 ├─ events(code, year, quarter PK)
                 ├─ prices(code, date PK)
                 └─ car(code, year, quarter PK)
features_pandas(code, year, quarter PK)   -- 대조 전용
```

복합 기본키가 "한 종목의 한 분기는 한 행"을 DB 차원에서 강제한다.
pandas만 쓸 때는 없던 보호 장치다.

**윈도우 함수로 파생 지표 재계산** — `LAG(revenue, 4) OVER (PARTITION BY code ORDER BY year, quarter)`.

추세이탈도는 pandas가 `numpy.polyfit`으로 직선을 적합했는데, SQL에서는 회귀를 돌릴 수 없다.
4점(x=0,1,2,3) 최소제곱해를 x=4로 외삽하면 계수가 정리되므로 가중합으로 대체했다.

```
예측 = -0.5·y(t-4) + 0.5·y(t-2) + 1.0·y(t-1)
```

검산: y=[1,2,3,4]이면 예측 5, y=[10,10,10,10]이면 예측 10. 모두 일치.

**교차 검증 결과** — 구현 경로가 전혀 다른 두 계산이 `1e-9` 이내로 일치했다.

```
[통과] 재무 패널 결측
[통과] 분기 연속성 (중간에 빠진 분기)
[통과] 이벤트-재무 연결
[통과] 영업이익률 재계산 대조
[통과] 주가 이상치 (±30% 초과)
[통과] 발표일이 거래일이 아닌 이벤트
[통과] pandas 대조 — 매출 성장률
[통과] pandas 대조 — 영업이익률 변화폭
[통과] pandas 대조 — 추세 이탈도
```

**SQL 집계로 재확인한 결과** (12단계 그림과 독립적으로 산출):

| 서프라이즈 그룹 | 건수 | 평균 CAR[0,+1] |
|---|---|---|
| 하위 (부정적) | 52 | −2.98% |
| 중위 | 52 | −0.86% |
| 상위 (긍정적) | 51 | +5.60% |

발표일 변동성 배수도 SQL로 재계산해 2.04배를 얻었다(pandas 2.03배).
차이는 SQLite에 표준편차 함수가 없어 모집단 표준편차 정의로 직접 계산했기 때문이며,
표본 범위도 v1으로 한정해 160건 기준이다. 실질적으로 동일한 결과다.

**DB 파일은 저장소에 두지 않는다.** `sql/*.sql`과 `14_build_db.py`로 언제든 재생성된다.

### 14.16 공개 전 점검 및 보완

공개 전환에 앞서 리뷰어 관점으로 점검해 다음을 보완했다.

| 항목 | 조치 |
|---|---|
| SQL 증거 부재 | 14.15 — SQL 레이어 구축 |
| `report.html`이 gitignore 처리됨 | 저장소에 포함. `figures/`와 동일하게 결과물로 취급 |
| `requirements.txt` 없음 | 추가 (하한 버전 명시, Python 3.12 검증) |
| `LICENSE` 없음 | MIT 추가 |
| 미사용 import | `11_regression.py`의 `numpy` 제거 |

남은 판단: EDA 과정(`09_validate_car.py`)이 콘솔 출력이라 실행하지 않으면 보이지 않는다.
노트북으로 옮기거나 출력을 문서로 저장하는 방안은 보류했다.
